Making this to automate a bunch of tasks, but mainly to periodically check the health of apps.

To install dependencies:

```sh
bun install
```

To run:

```sh
bun run dev
```

open http://localhost:3000
